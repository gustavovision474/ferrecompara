---
import { cn } from '@/lib/utils';

interface Props {
  class?: string;
}

const { class: className } = Astro.props as Props;

const classes = cn('max-w-7xl mx-auto px-6 md:px-12', className);
---

<div class={classes}>
  <slot />
</div>
